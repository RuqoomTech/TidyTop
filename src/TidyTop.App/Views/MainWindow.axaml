<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TidyTop.App.ViewModels"
        mc:Ignorable="d"
        x:Class="TidyTop.App.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="TidyTop"
        Width="1280"
        Height="820"
        CanResize="False"
        ShowInTaskbar="False"
        SystemDecorations="None"
        TransparencyLevelHint="Transparent"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        Background="Transparent">

    <Window.Styles>
        <Style Selector="Button.toolbar-button">
            <Setter Property="MinWidth" Value="76"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="Background" Value="#223244"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="11"/>
        </Style>
        <Style Selector="Button.icon-button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="Background" Value="#263449"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
        <Style Selector="Button.editor-button">
            <Setter Property="MinWidth" Value="92"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#F8FAFC"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#60A5FA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="11"/>
        </Style>
        <Style Selector="Button.editor-button.secondary">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#334155"/>
        </Style>
        <Style Selector="Button.editor-button.danger">
            <Setter Property="Background" Value="#7F1D1D"/>
            <Setter Property="BorderBrush" Value="#F87171"/>
        </Style>
    </Window.Styles>

    <Grid ClipToBounds="False" Background="Transparent">
        <ItemsControl Classes="smart-box-layer"
                      ItemsSource="{Binding SmartBoxes}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.Styles>
                <Style Selector="ItemsControl.smart-box-layer &gt; ContentPresenter" x:DataType="vm:SmartBoxViewModel">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    <Setter Property="IsVisible" Value="{Binding IsVisible}"/>
                </Style>
            </ItemsControl.Styles>

            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:SmartBoxViewModel">
                    <Border Width="{Binding Width}"
                            Height="{Binding Height}"
                            CornerRadius="18"
                            BorderThickness="1"
                            BorderBrush="#3DFFFFFF"
                            Background="#B80B1220"
                            ClipToBounds="True">
                        <Grid RowDefinitions="3,Auto,*">
                            <Border Grid.Row="0" Background="{Binding AccentBrush}"/>

                            <Border Grid.Row="1"
                                    Background="#57101A2B"
                                    Padding="10,8"
                                    Cursor="SizeAll"
                                    PointerPressed="OnSmartBoxHeaderPointerPressed"
                                    PointerMoved="OnSmartBoxInteractionPointerMoved"
                                    PointerReleased="OnSmartBoxInteractionPointerReleased"
                                    PointerCaptureLost="OnSmartBoxInteractionPointerCaptureLost">
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="9">
                                    <Border Width="32"
                                            Height="32"
                                            CornerRadius="11"
                                            Background="#18FFFFFF"
                                            BorderBrush="#24FFFFFF"
                                            BorderThickness="1"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Emoji}"
                                                   FontSize="15"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Spacing="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   Foreground="#F8FAFC"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding BoxSubtitle}"
                                                   Foreground="#9CA3AF"
                                                   FontSize="10"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <Border Grid.Column="2"
                                            Background="#1EFFFFFF"
                                            CornerRadius="999"
                                            Padding="8,4"
                                            VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ItemCount}"
                                                   Foreground="#F8FAFC"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <Button Grid.Column="3"
                                            Classes="icon-button"
                                            Content="⋯"
                                            ToolTip.Tip="{Binding MenuToolTip}"
                                            Click="OnSmartBoxEditClick"/>
                                </Grid>
                            </Border>

                            <Grid Grid.Row="2">
                                <ScrollViewer Padding="10,9,10,10"
                                              VerticalScrollBarVisibility="Auto"
                                              HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="vm:DesktopItemViewModel">
                                                <Border CornerRadius="14"
                                                        Background="#30172033"
                                                        BorderBrush="#1FFFFFFF"
                                                        BorderThickness="1"
                                                        Padding="8"
                                                        Margin="0,0,0,8"
                                                        Cursor="Hand"
                                                        ToolTip.Tip="{Binding FullPath}"
                                                        DoubleTapped="OnDesktopItemDoubleTapped"
                                                        PointerPressed="OnDesktopItemPointerPressed"
                                                        PointerMoved="OnDesktopItemPointerMoved"
                                                        PointerReleased="OnDesktopItemPointerReleased"
                                                        PointerCaptureLost="OnDesktopItemPointerCaptureLost">
                                                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="9">
                                                        <Border Width="34"
                                                                Height="34"
                                                                CornerRadius="12"
                                                                Background="#16FFFFFF"
                                                                BorderBrush="#22FFFFFF"
                                                                BorderThickness="1"
                                                                VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Icon}"
                                                                       FontSize="17"
                                                                       HorizontalAlignment="Center"
                                                                       VerticalAlignment="Center"/>
                                                        </Border>

                                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding Name}"
                                                                       Foreground="#F8FAFC"
                                                                       FontSize="12"
                                                                       FontWeight="SemiBold"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                            <TextBlock Text="{Binding DetailsText}"
                                                                       Foreground="#9CA3AF"
                                                                       FontSize="10"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                        </StackPanel>

                                                        <Button Grid.Column="2"
                                                                Classes="icon-button"
                                                                Content="↗"
                                                                ToolTip.Tip="Open"
                                                                HorizontalAlignment="Right"
                                                                VerticalAlignment="Center"
                                                                Click="OnDesktopItemOpenClick"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <Border IsVisible="{Binding IsEmpty}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="18"
                                        CornerRadius="18"
                                        Background="#18233446"
                                        BorderBrush="#24FFFFFF"
                                        BorderThickness="1"
                                        Padding="18,16">
                                    <StackPanel Spacing="7" HorizontalAlignment="Center">
                                        <TextBlock Text="Drop items here"
                                                   Foreground="#F8FAFC"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding EmptyText}"
                                                   Foreground="#9CA3AF"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"
                                                   MaxWidth="220"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <Border Grid.RowSpan="3"
                                    IsVisible="{Binding IsDropTarget}"
                                    Background="#334F9CFB"
                                    BorderBrush="#BF93C5FD"
                                    BorderThickness="2"
                                    CornerRadius="18"
                                    IsHitTestVisible="False">
                                <Border HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Background="#E60F172A"
                                        BorderBrush="#6693C5FD"
                                        BorderThickness="1"
                                        CornerRadius="999"
                                        Padding="16,9">
                                    <TextBlock Text="Drop to move here"
                                               Foreground="#EFF6FF"
                                               FontSize="13"
                                               FontWeight="SemiBold"/>
                                </Border>
                            </Border>

                            <Border Grid.RowSpan="3"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Width="24"
                                    Height="24"
                                    Margin="0,0,5,5"
                                    CornerRadius="8"
                                    Background="#D00F172A"
                                    BorderBrush="#3DFFFFFF"
                                    BorderThickness="1"
                                    Cursor="BottomRightCorner"
                                    ToolTip.Tip="Resize"
                                    PointerPressed="OnSmartBoxResizePointerPressed"
                                    PointerMoved="OnSmartBoxInteractionPointerMoved"
                                    PointerReleased="OnSmartBoxInteractionPointerReleased"
                                    PointerCaptureLost="OnSmartBoxInteractionPointerCaptureLost">
                                <TextBlock Text="◢"
                                           Foreground="#CBD5E1"
                                           FontSize="11"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Canvas IsHitTestVisible="False">
            <Border Canvas.Left="{Binding DragGhostX}"
                    Canvas.Top="{Binding DragGhostY}"
                    IsVisible="{Binding IsDraggingItem}"
                    CornerRadius="16"
                    Background="#F00F172A"
                    BorderBrush="#8093C5FD"
                    BorderThickness="1"
                    Padding="12,9">
                <StackPanel Spacing="3">
                    <TextBlock Text="{Binding DragGhostText}"
                               Foreground="#F8FAFC"
                               FontSize="12"
                               FontWeight="SemiBold"
                               MaxWidth="260"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding DragDropHint}"
                               Foreground="#BFDBFE"
                               FontSize="10"
                               MaxWidth="260"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>
        </Canvas>

        <Border HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Margin="18"
                CornerRadius="22"
                Background="#E00B1220"
                BorderBrush="#2FFFFFFF"
                BorderThickness="1"
                Padding="12,9">
            <StackPanel Orientation="Horizontal" Spacing="9" VerticalAlignment="Center">
                <Border Width="34" Height="34" CornerRadius="12" Background="#1EFFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1">
                    <TextBlock Text="🧹" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Spacing="0" VerticalAlignment="Center">
                    <TextBlock Text="TidyTop" Foreground="#F8FAFC" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding SummaryText}" Foreground="#94A3B8" FontSize="10"/>
                </StackPanel>
                <Border Background="#253244" Width="1" Height="28" Margin="4,0"/>
                <StackPanel Spacing="0" VerticalAlignment="Center" MinWidth="132">
                    <TextBlock Text="Desktop" Foreground="#CBD5E1" FontSize="11" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding DesktopIntegrationText}" Foreground="#94A3B8" FontSize="10"/>
                </StackPanel>
                <Border Background="#253244" Width="1" Height="28" Margin="4,0"/>
                <Button Classes="toolbar-button" Content="Refresh" Command="{Binding RefreshCommand}"/>
                <Button Classes="toolbar-button" Content="Add box" Command="{Binding AddSmartBoxCommand}"/>
                <Button Classes="toolbar-button" Content="Auto layout" Click="OnAutoArrangeClick"/>
                <Button Classes="toolbar-button" Content="{Binding NativeDesktopIconsButtonText}" IsEnabled="{Binding EnableNativeDesktopIconControl}" Click="OnToggleNativeDesktopIconsClick"/>
                <Button Classes="toolbar-button" Content="Restore icons" Click="OnRestoreNativeDesktopIconsClick"/>
                <Button Classes="toolbar-button" Content="Settings" Click="OnOpenSettingsClick"/>
                <Button Classes="toolbar-button" Content="{Binding OverlayVisibilityText}" Click="OnHideTidyTopClick"/>
                <Button Classes="toolbar-button" Content="Save" Command="{Binding SaveLayoutCommand}"/>
                <Button Classes="toolbar-button" Content="Reset" Command="{Binding ResetLayoutCommand}"/>
            </StackPanel>
        </Border>

        <Border HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="18"
                CornerRadius="16"
                Background="#D00B1220"
                BorderBrush="#2FFFFFFF"
                BorderThickness="1"
                Padding="12,8">
            <StackPanel Orientation="Horizontal" Spacing="9">
                <TextBlock Text="●" Foreground="#22C55E"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#CBD5E1" FontSize="12"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FCA5A5" FontSize="12" IsVisible="{Binding HasError}"/>
            </StackPanel>
        </Border>

        <Grid Background="#66000000" IsVisible="{Binding IsSmartBoxEditorOpen}">
            <Border Background="#F0111827"
                    BorderBrush="#3DFFFFFF"
                    BorderThickness="1"
                    CornerRadius="22"
                    Padding="22"
                    Width="430"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel Spacing="14">
                    <StackPanel Spacing="3">
                        <TextBlock Text="Edit SmartBox"
                                   Foreground="#F8FAFC"
                                   FontSize="18"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding EditingSmartBoxSubtitle}"
                                   Foreground="#94A3B8"
                                   FontSize="12"/>
                    </StackPanel>

                    <StackPanel Spacing="6">
                        <TextBlock Text="Name"
                                   Foreground="#CBD5E1"
                                   FontSize="12"
                                   FontWeight="SemiBold"/>
                        <TextBox Text="{Binding EditingSmartBoxTitle, Mode=TwoWay}"
                                 Watermark="SmartBox name"
                                 Foreground="#F8FAFC"
                                 Background="#1E293B"
                                 BorderBrush="#334155"
                                 BorderThickness="1"
                                 CornerRadius="12"
                                 Padding="12,9"/>
                    </StackPanel>

                    <TextBlock Text="Deleting a manual SmartBox never deletes desktop files. Items return to the automatic layout or Other / Unboxed."
                               Foreground="#94A3B8"
                               FontSize="11"
                               TextWrapping="Wrap"/>

                    <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="9">
                        <Button Classes="editor-button danger"
                                Content="Delete"
                                IsEnabled="{Binding EditingSmartBoxCanDelete}"
                                Command="{Binding DeleteSmartBoxCommand}"/>
                        <Button Grid.Column="2"
                                Classes="editor-button secondary"
                                Content="Cancel"
                                Command="{Binding CancelSmartBoxEditorCommand}"/>
                        <Button Grid.Column="3"
                                Classes="editor-button"
                                Content="Save"
                                Command="{Binding SaveSmartBoxEditorCommand}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>


        <Grid Background="#77000000" IsVisible="{Binding IsSettingsPanelOpen}">
            <Border Background="#F0111827"
                    BorderBrush="#3DFFFFFF"
                    BorderThickness="1"
                    CornerRadius="24"
                    Padding="0"
                    Width="780"
                    MaxHeight="720"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <DockPanel LastChildFill="True">
                    <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" Margin="22,20,18,14">
                        <StackPanel Spacing="4">
                            <TextBlock Text="Settings &amp; diagnostics"
                                       Foreground="#F8FAFC"
                                       FontSize="20"
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="Safe controls for desktop integration, emergency recovery, and runtime state."
                                       Foreground="#94A3B8"
                                       FontSize="12"/>
                        </StackPanel>
                        <Button Grid.Column="1"
                                Classes="icon-button"
                                Content="×"
                                Command="{Binding CloseSettingsCommand}"/>
                    </Grid>

                    <Grid DockPanel.Dock="Bottom" ColumnDefinitions="Auto,Auto,*,Auto,Auto" ColumnSpacing="10" Margin="22,14,22,22">
                        <Button Classes="editor-button danger" Content="Reset everything" Click="OnResetEverythingClick"/>
                        <Button Grid.Column="1" Classes="editor-button secondary" Content="Reset settings" Click="OnResetSettingsClick"/>
                        <TextBlock Grid.Column="2"
                                   Text="Reset actions never delete your real desktop files."
                                   Foreground="#94A3B8"
                                   FontSize="11"
                                   VerticalAlignment="Center"/>
                        <Button Grid.Column="3" Classes="editor-button secondary" Content="Close" Command="{Binding CloseSettingsCommand}"/>
                        <Button Grid.Column="4" Classes="editor-button" Content="Save settings" Click="OnSaveSettingsClick"/>
                    </Grid>

                    <ScrollViewer Margin="22,0,22,0" VerticalScrollBarVisibility="Auto">
                        <StackPanel Spacing="14">
                            <Border CornerRadius="18" Background="#99121A2A" BorderBrush="#2434475C" BorderThickness="1" Padding="16">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Runtime settings" Foreground="#F8FAFC" FontSize="15" FontWeight="SemiBold"/>
                                    <UniformGrid Columns="2">
                                        <CheckBox Content="Desktop overlay host" IsChecked="{Binding EnableDesktopOverlayHost, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,16,10"/>
                                        <CheckBox Content="Tray icon" IsChecked="{Binding EnableTrayIcon, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,0,10"/>
                                        <CheckBox Content="Global hotkey" IsChecked="{Binding EnableGlobalHotkey, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,16,10"/>
                                        <CheckBox Content="Drag and drop" IsChecked="{Binding EnableDragDrop, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,0,10"/>
                                        <CheckBox Content="Start hidden" IsChecked="{Binding StartHidden, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,16,10"/>
                                        <CheckBox Content="Auto-organize on refresh" IsChecked="{Binding EnableAutoOrganizeOnRefresh, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,0,10"/>
                                    </UniformGrid>
                                    <Grid ColumnDefinitions="160,*" ColumnSpacing="12">
                                        <TextBlock Text="Hotkey" Foreground="#CBD5E1" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <TextBox Grid.Column="1"
                                                 Text="{Binding GlobalHotkey, Mode=TwoWay}"
                                                 Watermark="Ctrl+Alt+T"
                                                 Foreground="#F8FAFC"
                                                 Background="#1E293B"
                                                 BorderBrush="#334155"
                                                 CornerRadius="12"
                                                 Padding="10,7"/>
                                    </Grid>
                                    <TextBlock Text="Note: the current hotkey engine supports Ctrl+Alt+T. The field is saved for future configurable hotkeys."
                                               Foreground="#94A3B8"
                                               FontSize="11"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <Border CornerRadius="18" Background="#99121A2A" BorderBrush="#2434475C" BorderThickness="1" Padding="16">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Native Windows desktop icons" Foreground="#F8FAFC" FontSize="15" FontWeight="SemiBold"/>
                                    <TextBlock Text="TidyTop only hides or shows Explorer's desktop icon view. It never deletes, moves, or edits your real desktop files."
                                               Foreground="#94A3B8"
                                               FontSize="11"
                                               TextWrapping="Wrap"/>
                                    <UniformGrid Columns="2">
                                        <CheckBox Content="Enable native icon control" IsChecked="{Binding EnableNativeDesktopIconControl, Mode=TwoWay}" Foreground="#CBD5E1" Margin="0,0,16,10"/>
                                        <CheckBox Content="Hide icons when TidyTop runs" IsChecked="{Binding HideNativeDesktopIcons, Mode=TwoWay}" IsEnabled="{Binding EnableNativeDesktopIconControl}" Foreground="#CBD5E1" Margin="0,0,0,10"/>
                                    </UniformGrid>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <Button Classes="editor-button secondary" Content="Restore Windows icons" Click="OnRestoreNativeDesktopIconsClick"/>
                                        <Button Classes="editor-button secondary" Content="Reset layout" Command="{Binding ResetLayoutCommand}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border CornerRadius="18" Background="#99121A2A" BorderBrush="#2434475C" BorderThickness="1" Padding="16">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Diagnostics" Foreground="#F8FAFC" FontSize="15" FontWeight="SemiBold"/>
                                    <Grid ColumnDefinitions="210,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto" RowSpacing="7" ColumnSpacing="12">
                                        <TextBlock Grid.Row="0" Text="Overlay" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OverlayDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="1" Text="Desktop host" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding OverlayHostDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="2" Text="Native icons" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding NativeIconsDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="3" Text="Tray" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding TrayDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="4" Text="Hotkey" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding HotkeyDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="5" Text="Layout file" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding LayoutDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="6" Text="Settings file" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SettingsDiagnosticText}" Foreground="#E2E8F0"/>
                                        <TextBlock Grid.Row="7" Text="Last error" Foreground="#94A3B8"/>
                                        <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding LastErrorDiagnosticText}" Foreground="#FCA5A5" TextWrapping="Wrap"/>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <Button Classes="editor-button secondary" Content="Open logs folder" Click="OnOpenLogsFolderClick"/>
                                        <Button Classes="editor-button secondary" Content="Open app data folder" Click="OnOpenSettingsFolderClick"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border CornerRadius="18" Background="#661E293B" BorderBrush="#2434475C" BorderThickness="1" Padding="14">
                                <TextBlock Text="Safety rule: keep either tray icon or global hotkey enabled if Start hidden is enabled, so you can always bring TidyTop back."
                                           Foreground="#CBD5E1"
                                           FontSize="11"
                                           TextWrapping="Wrap"/>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>

        <Grid Background="#33000000" IsVisible="{Binding IsBusy}">
            <Border Background="#E6111827"
                    CornerRadius="18"
                    Padding="24"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="🔄" FontSize="22"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontSize="15" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
